import { SearchVaccineModel } from './search-vaccine-model';

describe('SearchVaccineModel', () => {
  it('should create an instance', () => {
    expect(new SearchVaccineModel()).toBeTruthy();
  });
});
