import { SearchVaccineCollection } from './search-vaccine-collection';

describe('SearchVaccineCollection', () => {
  it('should create an instance', () => {
    expect(new SearchVaccineCollection()).toBeTruthy();
  });
});
